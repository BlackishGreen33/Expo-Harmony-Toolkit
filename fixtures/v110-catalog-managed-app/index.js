import { registerRootComponent } from 'expo';
import React from 'react';
import { Text } from 'react-native';

function App() {
  return <Text>v1.10 catalog managed fixture</Text>;
}

registerRootComponent(App);
