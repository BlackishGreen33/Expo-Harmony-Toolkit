# v1.11.2 验收记录（非实机 closeout 与 ccnubox simulator gate）

- 版本：`1.11.2`
- 发布轨：未发布；本记录完成 v1.11.2 发布前非实机 closeout，不创建 tag、不发布 npm、不创建 GitHub Release
- 日期：`2026-06-17`
- 范围：不依赖真机的 blocker accounting、release/runtime 缺口口径、bare / third-party 降级策略，以及 ccnubox_rn signed simulator app-shell gate

## 结论

`v1.11.2` 继续作为稳定工具链 / 文档 patch，不扩大 `verified`：

- `v1.11.1` 已经收口 repo 内可测的 `sidecar-drift` 工具链行为
- 本轮把剩余 v1.11.x blocker 写成可追踪台账与诊断口径，并补充 ccnubox_rn 的非实机 regression gate
- 当前本地 `ccnubox_rn` dirty checkout 已通过 `doctor/env/bundle/debug build/release build/signed simulator install/start` gate
- 不新增 CLI 命令
- 不修改 `DoctorReport`、`CapabilityDefinition` 或 `nextActions[]` wire shape
- 不把 debug/release HAP build pass 表述成真机 device pass 或 runtime pass
- 不把 preview / experimental capability 提升到 `verified`

## 非实机 burn-down 状态

| Track | v1.11.2 状态 | 后续出口 |
| --- | --- | --- |
| `release-evidence` | 四项 preview adapter capability 仍缺逐项 release HAP runtime acceptance | 每项 capability 单独关闭 release 记录；combined sample smoke 不可替代 |
| `bare-release-gap` | bare workflow 继续只有 intake / debug baseline | release HAP 与 runtime evidence 闭环前，不得宣称 release-ready |
| `third-party-native` | Wave A / Wave B 继续停留在 `experimental` | 关闭 device / release evidence；无法关闭时必须写明 fallback / replacement path |
| `sidecar-drift` | repo 内可测切片已由 v1.11.1 收口 | 真实项目 drift 若无法关闭，写入 accepted exception，不能替代 runtime evidence |

## ccnubox_rn 非实机 regression gate

目标 checkout：

- 路径：`/Users/blackishgreen03/workspace/ccnubox_rn`
- 分支：`main`
- HEAD：`066bf4a fix(pages): improve feedback page keyboard avoiding`
- 远端状态：`main...origin/main [behind 44]`
- 工作区状态：dirty checkout，保留现有 tracked / untracked Harmony 相关改动；本轮不 pull、不 reset、不清理下游改动

执行结果：

| Gate | 结果 |
| --- | --- |
| `node ../Expo-Harmony-Plugin/bin/expo-harmony.js doctor --target-tier experimental` | passed；`Eligibility: eligible`，`Coverage profile: managed-native-heavy` |
| `node ../Expo-Harmony-Plugin/bin/expo-harmony.js env` | passed；`Status: ready`，Signing `configured`，hdc 位于 DevEco SDK |
| `node ../Expo-Harmony-Plugin/bin/expo-harmony.js bundle` | passed；生成 `harmony/entry/src/main/resources/rawfile/bundle.harmony.js` |
| `node ../Expo-Harmony-Plugin/bin/expo-harmony.js build-hap --mode debug` | passed；生成 signed / unsigned HAP artifacts |
| `node ../Expo-Harmony-Plugin/bin/expo-harmony.js build-hap --mode release` | passed；生成 signed / unsigned HAP artifacts |
| `/Applications/DevEco-Studio.app/Contents/sdk/default/openharmony/toolchains/hdc list targets` | passed；启动 emulator 后 target 为 `127.0.0.1:5555` |
| `hdc install -r harmony/entry/build/default/outputs/default/entry-default-signed.hap` | passed；`install bundle successfully` |
| `hdc shell aa start -a EntryAbility -b com.muxixyz.ccnubox` | passed；`start ability successfully` |

补充记录：

- 初次 `hdc list targets` 返回 `[Empty]`，随后使用既有 DevEco emulator profile `Enjoy 90 Pro Max` 拉起目标
- emulator command：`/Applications/DevEco-Studio.app/Contents/tools/emulator/Emulator -hvd "Enjoy 90 Pro Max" -path "/Users/blackishgreen03/.Huawei/Emulator/deployed" -t trace_44577_commandPipe -imageRoot "/Users/blackishgreen03/Library/Huawei/Sdk"`
- build / bundle 期间仍按 drift policy 跳过 `harmony/entry/src/main/module.json5`、`harmony/entry/src/main/resources/base/element/string.json`、`metro.harmony.config.js`、`index.harmony.js`，warning code 为 `sidecar.drift.requires-force`
- signed simulator install/start 只证明 ccnubox app-shell 非实机 smoke gate，不替代真机 device acceptance，也不关闭单 capability release runtime evidence

## 降级与替代路径

- preview adapter capability：继续保留 `preview`，对外只承诺 bundle/debug build baseline 与已有人工记录；release HAP runtime 未关闭前不进入 `verified`
- bare workflow：继续保留 `coverageProfile=bare` intake path，保留 native directories，使用 `doctor` 分类；release HAP/runtime 未关闭前不称为 release-ready
- Third-party Wave A：继续要求 `doctor --target-tier experimental`，async-storage / screens 走 Harmony adapter，safe-area 走 toolkit shim；证据关不掉时记录替代依赖或功能降级路径
- Third-party Wave B：继续要求 `doctor --target-tier experimental`，WebView / media-library / Lottie / Skia 走 adapter，JPush runtime evidence 单独记录；证据关不掉时按 surface 写出 fallback
- catalog 外 native dependency：继续按 `third-party-native-gap` 报出，不能降写成普通 matrix drift

## 验收边界

- 本记录不要求真机 device evidence
- 本记录不关闭 release HAP runtime evidence
- 本记录不发布 `1.11.2`
- 当前 npm `latest` 与 GitHub Release 仍以已发布版本为准，直到后续显式发版
- ccnubox signed simulator app-shell pass 不改变任何 preview / experimental capability 的 `evidence.release=no[none]`

## 验证命令

```bash
node ../Expo-Harmony-Plugin/bin/expo-harmony.js doctor --target-tier experimental
node ../Expo-Harmony-Plugin/bin/expo-harmony.js env
node ../Expo-Harmony-Plugin/bin/expo-harmony.js bundle
node ../Expo-Harmony-Plugin/bin/expo-harmony.js build-hap --mode debug
node ../Expo-Harmony-Plugin/bin/expo-harmony.js build-hap --mode release
/Applications/DevEco-Studio.app/Contents/sdk/default/openharmony/toolchains/hdc list targets
/Applications/DevEco-Studio.app/Contents/sdk/default/openharmony/toolchains/hdc install -r harmony/entry/build/default/outputs/default/entry-default-signed.hap
/Applications/DevEco-Studio.app/Contents/sdk/default/openharmony/toolchains/hdc shell aa start -a EntryAbility -b com.muxixyz.ccnubox
pnpm exec jest --runInBand tests/docs.test.ts tests/doctor.test.ts
pnpm docs:sync
git diff --check
pnpm build
pnpm test
EXPO_HARMONY_RELEASE_SKIP_HAP=1 pnpm release:check
npm pack --dry-run
```
