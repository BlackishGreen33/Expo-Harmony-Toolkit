# `expo-image-picker` Release Acceptance

- 状态：`pending external release HAP run`
- 当前基线：release evidence 尚未建立
- Route：`/image-picker`
- 目标：确认 release HAP 下图库与系统相机流程可用，且权限行为不回退

## Repo 内已完成前置条件

- route walkthrough：`/image-picker`
- bundle slice：library selection、capture、pending result 与 metadata 展示已进入 preview sample bundle 检查
- release HAP 前置：release signing、HAP 安装启动与该 route 的 runtime 行为仍需外部验收
- `v1.8.2` app-shell simulator release HAP 证据没有携带 `READ_IMAGEVIDEO` 的单 capability runtime 验收，不可替代本记录

## 待填 release 记录

- signing profile：
- 设备 / 模拟器目标：
- HAP artifact：
- 验收时间：
- 执行人：
- 结果：`pass` / `fail`
- 安装 / 启动命令输出：
- 失败日志或截图：

## 必验场景

- release signing 配置生效
- release HAP 安装与启动
- library selection / capture / pending result
- denied / canceled / successful result 全部稳定
