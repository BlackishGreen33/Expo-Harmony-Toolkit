# v1.11.4 验收记录（publication-state reconciliation）

- 版本：`1.11.4`
- 发布轨：`latest`
- 范围：对账 `v1.11.3` 的发布状态，更新版本与长期发布文件，不扩大 `verified` 或 public API

## 结论

`v1.11.4` 是 publication-state reconciliation patch：它把 package、toolkit 常数、双语 README、roadmap、support matrix、CLI 构建说明与 npm 发布说明对齐到同一版本口径，同时把易腐化的精确远端状态移入版本 acceptance。

- 不新增 CLI 命令
- 不修改 `DoctorReport`、`CapabilityDefinition` 或 `nextActions[]` wire shape
- 不把 preview / experimental capability 提升到 `verified`
- 不把 debug/release HAP build pass、simulator install/start 或 app-shell smoke 表述成真机 device pass 或 per-capability runtime pass

## 发布状态对账

| Surface | `v1.11.3` 对账结果 | `v1.11.4` 记录 |
| --- | --- | --- |
| 初始 readiness | 未创建 tag、发布 npm 或创建 GitHub Release | 历史状态保留在 `v1.11.3-acceptance.md` |
| 后续发布 | security commit 后已发布 npm、tag 与 GitHub Release | 不重打或移动 `v1.11.3` tag |
| 长期发布文件 | 不保留会腐化的 npm dist-tag 或“尚未发布”现况句 | 精确远端状态只留在 acceptance |

## 本地验证证据

以下命令在本地 release 对账中已执行；结果只记录可复现的摘要，不记录临时目录或环境凭证：

| 命令 | 结果 |
| --- | --- |
| `pnpm exec jest --runInBand tests/docs.test.ts` | `Test Suites: 1 passed, 1 total`；`Tests: 6 passed, 6 total`。 |
| `pnpm build` | TypeScript build 通过。 |
| `pnpm pack:check` | dry-run tarball 为 `expo-harmony-toolkit@1.11.4`，`entryCount: 130`，不含禁止的 `acceptance/`、`examples/`、`fixtures/`、`tests/` 前缀。 |
| `EXPO_HARMONY_RELEASE_SKIP_HAP=1 pnpm release:check` | `exit 0`；完成 build、完整 Jest，以及 tarball smoke 的 `doctor --strict`、`init --force`、`bundle`。 |
| `pnpm test` | `exit 0`；完整 Jest 子程序结束。 |
| `git diff --check` | 通过；无 whitespace error。 |

`release:check` 以 `EXPO_HARMONY_RELEASE_SKIP_HAP=1` 执行时只跳过真实 DevEco HAP 构建；其余 tarball 安装 smoke、`doctor --strict`、`init --force` 与 `bundle` 仍已完成。

## 发布证据

以下远端发布证据已经完成对账：

| Surface | 实际证据 |
| --- | --- |
| Git tag | `v1.11.4` 指向 `ab01e02dbbe28bdcf3af6062d869007409b40997`。 |
| npm | `expo-harmony-toolkit@1.11.4` 已发布；`latest -> 1.11.4`；`next -> 1.9.0`。 |
| GitHub Release | [v1.11.4](https://github.com/BlackishGreen33/Expo-Harmony-Toolkit/releases/tag/v1.11.4)；`publishedAt=2026-08-15T19:25:49Z`；`draft=false`；`prerelease=false`。 |
| Release workflow | [run 31903698914](https://github.com/BlackishGreen33/Expo-Harmony-Toolkit/actions/runs/31903698914) `success`，`31903698914` success，`head=ab01e02dbbe28bdcf3af6062d869007409b40997`。 |
| Main CI | `31903434400` success。 |
| Pull request | PR `#3` 已 merge。 |

## 验收边界

- 本记录不要求真机 device evidence
- 本记录不关闭 release HAP runtime evidence
- 本记录不改变 npm `latest` / `next` 的 capability promotion 规则
- `v1.11.3` 的初始 readiness 与后续发布状态都保留在其自身 acceptance，不重写历史
