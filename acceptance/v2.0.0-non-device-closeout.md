# v2.0.0-next.0 非實機 closeout 驗收記錄

- Package：`expo-harmony-toolkit@2.0.0-next.0`
- Tarball：`expo-harmony-toolkit-2.0.0-next.0.tgz`
- Tarball SHA-256: `47f9f95661f155a7f7c484a0c503824df4030b5cab816c2b1330aa7a732a7dde`
- 日期：`2026-08-16`
- 範圍：`5 lane groups / 7 physical projects` 的 packed portable、debug HAP、release HAP、signed simulator basic runtime 與 19 條 route 非實機證據
- Publication status：`未发布`；本記錄不代表 npm、Git tag 或 GitHub Release 已建立
- 執行型態：local-only，沒有 CI run id；controller 完成獨立 review 與最終 gate 後才可發布

## 結論與邊界

七個 fresh-materialized project 都從同一個 packed tarball 安裝 toolkit，通過 parsed doctor、forced init、兩次 forced sync、bundle marker、debug HAP 與 release HAP build。七個 project 也都取得 simulator basic-runtime pass；其中 `official-native-capabilities` 與 `official-wave-b` 的原始 signed release HAP 因 signing profile 無法授予 restricted permission 而安裝 blocked，simulator 欄位的 pass 來自明確標記的 temp-only permission-removed variant。

Route 層已執行 catalog 全部 19 條 route，19/19 均看到 exact route marker、process 與 ability 存活，且 route 後 fatal runtime error count 為 0。這不等於 19 條 functional action 全部通過：17 條 route 的功能或指定 fallback 通過；`file-system` 的 write/read/cleanup action 與 Wave B media-library permission action仍為 blocked。

`simulator pass 不是 device pass`。本記錄不提升任何 public support tier，不修改 `DoctorReport`、`CapabilityDefinition`、public wire format 或 `CapabilityEvidence.release/device`。相機擷取、真實相簿選取、GPS、推播到達／點擊、真實 media-library 讀寫、原生 screens 與 Skia surface 等真機語義保持 deferred（文件契約：`真机语义保持 deferred`）。

## Project matrix

狀態只表示本記錄的 packaging/build/basic-runtime 欄位；capability route 的 action 結果另見下一節。

| Lane group | Project | portable | debugHap | releaseHap | simulator |
| --- | --- | --- | --- | --- | --- |
| managed-verified | official-minimal | pass | pass | pass | pass |
| managed-verified | official-app-shell | pass | pass | pass | pass |
| managed-verified | official-ui-stack | pass | pass | pass | pass |
| preview-foundation | official-native-capabilities | pass | pass | pass | pass |
| bare | official-bare | pass | pass | pass | pass |
| wave-a | official-wave-a | pass | pass | pass | pass |
| wave-b | official-wave-b | pass | pass | pass | pass |

補充：

- `official-native-capabilities` 原始 signed release HAP 安裝被 `ohos.permission.READ_IMAGEVIDEO` 阻擋；temp-only simulator variant只移除這項 permission，證明 app-shell、route fallback/error path 與 stability，不代表 image-picker 或 media native success。
- `official-wave-b` 原始 signed release HAP 安裝被 `ohos.permission.NOTIFICATION_CONTROLLER` 阻擋；temp-only simulator variant移除 push/media restricted permissions，只證明 basic runtime 與 fallback/stability，不代表 push 或 media native success。
- 其餘五個 project 使用原始 signed release HAP完成 scoped clean install、start、EntryAbility/process、project marker 與 fatal=0 檢查。

## Route-level simulator evidence

`executed/stability=pass` 表示 UI 導航後 exact route marker 可見、process/ability alive 且 fatal=0。`action` 欄不以 bundle marker 冒充實際 callback。

| Project | Route | executed/stability | action | Simulator evidence / boundary |
| --- | --- | --- | --- | --- |
| official-native-capabilities | `/asset` | pass | pass | `Asset.fromURI` metadata result可辨識；未宣稱 remote network download |
| official-native-capabilities | `/camera` | pass | pass | permission snapshot可讀；camera hardware capture deferred |
| official-native-capabilities | `/clipboard` | pass | pass | session shim write/read result可辨識；force-stop後不保留測試值 |
| official-native-capabilities | `/device` | pass | pass | device type callback result可辨識 |
| official-native-capabilities | `/file-system` | pass | blocked | directory create pass；UTF-8 write、read full與cleanup未取得 callback result |
| official-native-capabilities | `/haptics` | pass | pass | `selectionAsync` callback成功；真機觸覺語義 deferred |
| official-native-capabilities | `/image-picker` | pass | pass | permission denied/error path穩定；相簿選取與內容語義 deferred |
| official-native-capabilities | `/location` | pass | pass | provider-disabled狀態可讀；GPS定位語義 deferred |
| official-native-capabilities | `/secure-store` | pass | pass | availability result可辨識；未寫入測試 secret |
| official-wave-a | `/third-party-wave-a/async-storage` | pass | pass | roundtrip value=`ok`，action自行移除測試 key |
| official-wave-a | `/third-party-wave-a/gesture-handler` | pass | pass | adapter route marker與documented lane可見 |
| official-wave-a | `/third-party-wave-a/safe-area` | pass | pass | documented limitation可見，不宣稱完整 native inset |
| official-wave-a | `/third-party-wave-a/screens` | pass | pass | screens fallback `enableScreens(false)` 可見；不是 native screens success |
| official-wave-b | `/third-party-wave-b/jpush` | pass | pass | push fallback 可見；未註冊或接收真實 push |
| official-wave-b | `/third-party-wave-b/lottie` | pass | pass | covered route render穩定；真機播放語義未擴張 |
| official-wave-b | `/third-party-wave-b/media-library` | pass | blocked | action後初始文字消失，但沒有可辨識 status/error，也沒有 permission dialog |
| official-wave-b | `/third-party-wave-b/notifications` | pass | pass | push fallback 可見；不是通知 native success |
| official-wave-b | `/third-party-wave-b/skia` | pass | pass | Skia fallback 可見；不是 native Skia surface success |
| official-wave-b | `/third-party-wave-b/webview` | pass | pass | covered route render穩定；未擴張到真機 navigation/message semantics |

### file-system blocked evidence

只使用有界且互不重複的 automation 方法，沒有盲目重跑：

1. 初始 UI runner在捲動後點擊 `Write UTF-8 file`；按鈕列仍有 scroll movement，沒有可辨識 result text，立即停止該 action chain。
2. 精準 correction先等待 bounds 連續穩定；Write clickable父節點連續兩次為 `[162,672][1110,812]`，viewport為 `1272×2772`，單次 tap `(636,742)` 完整落在 visible/enabled/clickable父節點內。tap後按鈕列上移136px且無新增或移除 result text。另獲准的 cleanup-only單次 tap，其父節點為 `[162,1864][1110,2011]`、tap `(636,1938)`，結果同樣無 `Cleanup OK`；不再重試座標點擊。
3. SDK-documented keyboard method送出兩次 `KEYCODE_TAB=2049` 並逐次 dump；focus始終停在同一 root node，沒有可觀察 focus變化，因此沒有送出 `KEYCODE_ENTER=2054`。

第一步只證明 sandbox directory建立成功。UTF-8 write、read full與sandbox cleanup未證，不得寫成 functional pass。保留的 temp evidence包含 stable/result UI tree、focus probe與tap後 screenshot；第一個 correction 的tap前 screenshot未由 runner捕捉，只有tap前 stable UI tree，這是明確 evidence gap。

### media-library blocked evidence

`Inspect media permission` 點擊前顯示 `Permission lookup has not run.`；點擊後該文字消失，但沒有出現 `Media permission status=...`、可辨識 error/cancel/denied文字或 system permission dialog。Route marker、process、ability與fatal=0仍通過，因此只記 route/stability pass、permission action blocked，不修改 source/HAP來美化本輪證據。

## Artifact SHA-256

下列 artifacts 全部來自 final candidate tarball；HAP只保存在未提交的單一 temp root。

| Project | Artifact | Signed | Artifact SHA-256 |
| --- | --- | --- | --- |
| official-minimal | debug `entry-default-unsigned.hap` | no | `d427989045336912be5931017ac8c5e203c0f044778210c588744769ca65cd7f` |
| official-minimal | release `entry-default-signed.hap` | yes | `b4c532ef8e942c3894fcb1e348d8db090332f158a633f0bcd2a7de68861a99c3` |
| official-minimal | release `1-entry-default-unsigned.hap` | no | `634a7e9eda1cfa09a29b0e56c95f683eb9fae800e7239ecff1b2341df7f00338` |
| official-app-shell | debug `entry-default-unsigned.hap` | no | `ad83d61c9bcad8d8a378e3816c2dc1d201e0befb5e96b9a6406edc22142d74bd` |
| official-app-shell | release `entry-default-signed.hap` | yes | `9300545d4aeb51e4c17ffe6b32faee965cd39943292edbdf4c9e19dffd7f5050` |
| official-app-shell | release `1-entry-default-unsigned.hap` | no | `54d157afcf8752a39f9c8d753363ecdc54b3e531f1b6c7be682afb87d150c36c` |
| official-ui-stack | debug `entry-default-unsigned.hap` | no | `a6c4c9d1ebc09dfe0046ec51af71860230f99accc43af003d0ec22ad380a0961` |
| official-ui-stack | release `entry-default-signed.hap` | yes | `c5afcaff5fe056515ad891fe6f320fde237788acb5f3afabf7be21841debc792` |
| official-ui-stack | release `1-entry-default-unsigned.hap` | no | `8273ec5d1f76535f9b59e9a979dd971a9c13200b10ae36fd90aa684ed898abf6` |
| official-native-capabilities | debug `entry-default-unsigned.hap` | no | `798be275270dd7bde0ad73aa1567597fddfb2f03a9630a5e2a3caf98eb1991b8` |
| official-native-capabilities | original release `entry-default-signed.hap` | yes | `b7711e74fa2c31627bc567e174ff80c326393780a661231a6a98aa20e519181c` |
| official-native-capabilities | release `1-entry-default-unsigned.hap` | no | `8377df553e1312f0229cf42fafb21d908583f9f5c237c9808031c49f9b18b11d` |
| official-native-capabilities | temp-only `entry-default-simulator-signed.hap` | yes | `f2ccae5d8334ac0c3432f6b482e902df7f7a403b2c710874ec6f3cc374ac0618` |
| official-bare | debug `entry-default-unsigned.hap` | no | `aa430094c5f833dc482c513f8c6e26800178ac48af48c260c12c9e825a55e8c4` |
| official-bare | release `entry-default-signed.hap` | yes | `d67b76353ad8bce5c5328dee4ae03815eabdd300396c65e060901ac0f1fb4d32` |
| official-bare | release `1-entry-default-unsigned.hap` | no | `db8f16a1954b02fb1b39554df410023a451f8756a4878ed0939ecd2bf4d58b55` |
| official-wave-a | debug `entry-default-unsigned.hap` | no | `832539a865b0d8460c40b2ff9089f39c503091a81eff939e4c8f680f532265fe` |
| official-wave-a | release `entry-default-signed.hap` | yes | `230764f465048ff54ee99479087c64bfef320487dc99d4e196a37d1713fe2f3e` |
| official-wave-a | release `1-entry-default-unsigned.hap` | no | `a7173589244977cd27035d961102447a5b6c55093c6834e449241819542a05e0` |
| official-wave-b | debug `entry-default-unsigned.hap` | no | `d5a6932d8c399fa3e777e19622429c310a87e8e3cc76392599670c184b57115b` |
| official-wave-b | original release `entry-default-signed.hap` | yes | `a0177695be8725c1887c4919be061f576f7c20fb62f4fc403b856d2a6b0dde22` |
| official-wave-b | release `1-entry-default-unsigned.hap` | no | `91cbf94e5f25fd33cd7f979f6c4c4488488cbc907b96da10f0c6b045aa694895` |
| official-wave-b | temp-only `entry-default-simulator-signed.hap` | yes | `d39271309a8d00ff3b6dc199479eddf84f969ccc3b252bcedf0e4a790101e8dc` |

## Fresh-build provenance 與不採信證據

- Final candidate 的七個 project都從 source sample fresh materialize到新短路徑；沒有沿用舊 `harmony/.cxx`、`build` 或 HAP artifacts。共用 pnpm store不改變 project build cache freshness。
- 每個 project安裝後都直接檢查 installed toolkit中的 final tarball contract；screens alias/app-key entry與實際 capability相符，再執行 init/sync/bundle/HAP。
- Final candidate以前的 tarball、HAP、runtime與重用 build cache結果都移到 `superseded/` 並明確不採信。被中斷或同時存在的非 authoritative Wave A runner output也只保留為診斷，不進入本表。
- 重用舊 native cache曾造成 appspawn SIGSEGV；同一 HAP control與fresh materialize對照證明為 stale-cache harness issue。Final matrix全部使用fresh materialize結果。
- `examples/` 與 `fixtures/` 在執行後保持 source clean；debug/release build後的可版本化 build profile沒有殘留 password fields。

## Audit 分流

兩個 audit surface保持分離，不用總數或 allowlist互相抵銷：

- packed consumer：安裝實際 packed tarball的 production graph，publish hard gate要求並取得 `critical=0`。
- workspace/examples：開發與sample graph仍有兩個既有 upstream critical，分別是 `GHSA-w7jw-789q-3m8p`（root Expo / React Native CLI的 `shell-quote` path）與 `GHSA-23hp-3jrh-7fpw`（official app-shell RNOH CLI的 `tar` path）。它們不是 packed consumer critical，也沒有被隱藏或改寫成 allowlist。

## 工具與可重現命令

本地工具版本：Node `20.19.6`、pnpm `10.32.1`、DevEco Studio `6.0.2`、ohpm `6.0.1`、hdc `3.2.0c`。Signing只驗證 config key structure、material existence與build後secret restore；本記錄不包含任何password、key、profile/certificate內容或target id。

```bash
# 單次pack；七個project重用同一路徑與同一SHA-256
pnpm pack --pack-destination <temp-root>/final/pack
pnpm add --ignore-scripts --save-dev <temp-root>/final/pack/expo-harmony-toolkit-2.0.0-next.0.tgz

# 每project portable/build contract
pnpm exec expo-harmony doctor --project-root . --target-tier <tier> --json
pnpm exec expo-harmony init --project-root . --force
pnpm exec expo-harmony sync-template --project-root . --force
pnpm exec expo-harmony sync-template --project-root . --force
pnpm exec expo-harmony bundle --project-root .
pnpm exec expo-harmony build-hap --project-root . --mode debug
pnpm exec expo-harmony env --json
pnpm exec expo-harmony build-hap --project-root . --mode release

# simulator basic runtime與route證據；實際bundle/ability由temp identity讀取，不寫入本記錄
hdc install -r <signed-release-or-bounded-simulator-variant.hap>
hdc shell aa start -a EntryAbility -b <temp-bundle>
hdc shell pidof <temp-bundle>
hdc shell aa dump -r
hdc shell uitest dumpLayout -p <remote-layout> -b <temp-bundle>
hdc shell hilog -x -P <pid>

# repository final gates
pnpm exec jest --runInBand tests/docs.test.ts tests/build.test.ts
pnpm build
pnpm test
pnpm pack:check
EXPO_HARMONY_RELEASE_CHANNEL=next EXPO_HARMONY_RELEASE_SKIP_HAP=1 CI=1 pnpm release:check
```

## Publication 與後續 gate

- Final-HEAD驗證：`pnpm build` pass；`pnpm test` 17 suites / 135 tests pass；coverage statements `87.76%`、lines `87.89%`，exit 0；`pnpm pack:check` pass，133 entries。
- `EXPO_HARMONY_RELEASE_CHANNEL=next EXPO_HARMONY_RELEASE_SKIP_HAP=1 CI=1 pnpm release:check` exit 0；內含build、17/135 tests、packed consumer 481 dependencies / `critical=0`、7/7 project portable matrix。每個project的第二次sync均為 `written=0` / `skipped=0`，marker-bearing bundle通過；HAP只按明確環境旗標skip，沒有跳過portable project。
- Final-HEAD重新pack的archive bytes與本記錄HAP matrix所用tarball完全一致，SHA-256仍為 `47f9f95661f155a7f7c484a0c503824df4030b5cab816c2b1330aa7a732a7dde`。
- `2.0.0-next.0` 在本記錄中維持未發布；沒有 push、tag、npm publish或GitHub Release mutation。
- Controller需在獨立task review、whole-branch review、PR/CI與final-HEAD release gate通過後，才發布npm `next`、Git tag與GitHub prerelease，並另做post-release publication reconciliation。
- 穩定版 `2.0.0` 不在本輪範圍；真機與各capability硬體語義仍是獨立deferred gate。
