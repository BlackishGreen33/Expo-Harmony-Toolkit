# `expo-file-system` Release Acceptance

- 状态：`pending external release HAP run`
- 当前基线：release evidence 尚未建立
- Route：`/file-system`
- 目标：在签名与 release HAP 路径下验证文件系统能力不回退

## Repo 内已完成前置条件

- route walkthrough：`/file-system`
- bundle slice：sandbox I/O、md5、download 主路径已进入 preview sample bundle 检查
- release HAP 前置：release signing、HAP 安装启动与该 route 的 runtime 行为仍需外部验收
- `v1.8.2` app-shell simulator release HAP 证据只覆盖最低安装启动链路，不可替代本记录

## 待填 release 记录

- signing profile：
- 设备 / 模拟器目标：
- HAP artifact：
- 验收时间：
- 执行人：
- 结果：`pass` / `fail`
- 安装 / 启动命令输出：
- 失败日志或截图：

## 必验场景

- release signing 配置生效
- release HAP 安装与启动
- sandbox I/O、md5、download 主路径
- release 模式下不出现 preview-only 崩溃或权限回退
