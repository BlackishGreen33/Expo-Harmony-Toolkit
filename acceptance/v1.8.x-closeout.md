# v1.8.x 收尾记录（repo-only closeout）

- 范围：只收口不依赖真机或 release HAP 的 v1.8.x 工作
- 发布：不发版、不打 tag、不修改 package version
- 结论：可以进入 v1.9.x 主线，但不能把任何 preview capability 提升到 `verified`

## 已收口

- 四项 preview capability 的 route walkthrough、bundle slice、permission / shim 与 debug build evidence source 已进入 repo 内可验证状态
- `doctor-report.json` 与 `toolkit-config.json` 继续公开 `runtimeMode`、`evidence`、`evidenceSource`、`coverageProfile` 与 `nextActions`
- combined sample smoke 继续负责回归，单 capability acceptance 继续负责晋升证据
- roadmap、support matrix 与 capability board 已同步表达：v1.8.x repo 内收尾完成，外部证据顺延

## 仍待外部验收

- `expo-file-system`、`expo-image-picker`、`expo-location`、`expo-camera` 的单 capability 真机 device record
- 四项 preview capability 的单 capability release HAP runtime acceptance
- 任何需要真机、生产签名、AppGallery profile 或 release HAP 安装启动的证据

## 边界

- `latest` 仍只承接完整验收的 `verified`
- `device=yes[manual-doc]` 只代表既有人工设备基线，不代表本轮新增了自动化设备验证
- `release=no[none]` 保持不变；`v1.8.2` app-shell simulator release HAP 证据不得替代单 capability release acceptance
- 本轮不修改 `src/data/capabilities.ts`，因此 `doctor-report.json` 与 `toolkit-config.json` 的能力 evidence 口径不变

## 本轮验证

- `pnpm build`
- `pnpm exec jest --runInBand tests/docs.test.ts tests/doctor.test.ts tests/officialNativeCapabilitiesSample.test.ts`
- `pnpm docs:sync`
- `git diff --check`
