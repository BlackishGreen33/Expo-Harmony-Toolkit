# `expo-image-picker` Device Acceptance

- 状态：`pending external device run`
- 当前基线：`bundle=yes[automated]`、`debugBuild=yes[automated]`、`device=yes[manual-doc]`、`release=no[none]`
- Route：`/image-picker`
- 目标：把图库、拍照、拍视频与 pending result 分别固化成单 capability device 记录

## Repo 内已完成前置条件

- route walkthrough：`/image-picker`
- bundle slice：library selection、capture、pending result 与 metadata 展示已进入 preview sample bundle 检查
- permission / shim：`ohos.permission.CAMERA`、`ohos.permission.MICROPHONE`、`ohos.permission.READ_IMAGEVIDEO` 由 toolkit 生成；`expo-image-picker` shim 由 toolkit 生成
- debug build evidence source：`debugBuild=yes[automated]`，来源为 preview sample / build gate，不代表真机验收

## 待填设备记录

- 设备型号：
- HarmonyOS / OpenHarmony 版本：
- HAP 类型：debug
- 验收时间：
- 执行人：
- 结果：`pass` / `fail`
- 失败日志或截图：

## 必验场景

- media permission / camera permission
- 单选图库
- 多选图库
- mixed image + video library selection
- system photo capture / system video capture
- pending result 恢复
