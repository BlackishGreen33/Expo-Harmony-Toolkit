# v1.11.3 验收记录（v2 readiness 非实机收口）

- 版本：`1.11.3`
- 发布轨：`latest`；初始 readiness 记录与后续发布状态均在本文件保留
- 日期：`2026-06-18`
- 范围：把 v2 前剩余 blocker 收敛成明确 gate、fallback、exception 与公开边界，不依赖真机，不关闭 per-capability release runtime evidence

## 结论

`v1.11.3` 是 v2 readiness 的非实机收口 patch，不扩大 `verified`：

- 不新增 CLI 命令
- 不修改 `DoctorReport`、`CapabilityDefinition` 或 `nextActions[]` wire shape
- 不把 preview / experimental capability 提升到 `verified`
- 不把 debug/release HAP build pass、simulator install/start 或 app-shell smoke 表述成真机 device pass 或 per-capability runtime pass
- 初始 readiness 记录时尚未创建 tag、发布 npm 或创建 GitHub Release；当时 `v1.11.2` 仍是已发布的 tag/npm/GitHub Release surface

## 发布状态对账

- 初始 readiness 记录只更新仓库代码与文档台账，不改变 npm `latest` / `next` dist-tag 状态
- 后续 security commit 后已发布 npm、tag 与 GitHub Release；本地 `v1.11.3` tag 指向 security commit `489b500a5cced42b8193af2c5251684ba3f79207`
- 该发布只交付稳定工具链 / 文档 patch，不改变 true-device、per-capability runtime 或 `verified` 要求

## v2 readiness gate

| Track | v1.11.3 处理 | v2 前出口 |
| --- | --- | --- |
| `release-evidence` | 四项 preview adapter capability 继续保持 `release=no[none]` | 每项 capability 单独补 release HAP runtime acceptance；combined sample smoke 不可替代 |
| `bare-release-gap` | bare workflow 继续保留 intake / debug baseline | release HAP 与 runtime evidence 闭环前，只能称 intake-supported，不能称 release-ready |
| `third-party-native` | Wave A / Wave B 继续保持 `experimental` | 逐项补 device / release evidence；无法关闭时记录 fallback / replacement path |
| `sidecar-drift` | repo 内可测行为沿用 `sidecar.drift.requires-force` 与 build-required auto-refresh | 真实项目 drift 若仍无法关闭，写入 accepted exception，不能替代 runtime evidence |
| `v2-boundary` | v2 承诺收敛为 catalog-covered Expo projects reliable packaging | catalog 外依赖只承诺 doctor intake、blocker 分类与有序 nextActions |

## 降级与替代路径

- preview adapter capability：保持 `preview`，对外只承诺 bundle/debug build baseline 与已有人工记录；release HAP runtime 未逐项关闭前不进入 `verified`
- app foundation modules：保持 `preview` + `runtimeMode=shim`，不宣称 native parity
- bare workflow：保持 `coverageProfile=bare` intake path，保留 native directories，并继续使用 `doctor` 输出 release/runtime blocker
- Third-party Wave A：async-storage / screens 走 Harmony adapter，safe-area 走 toolkit shim；证据关不掉时记录替代依赖或功能降级
- Third-party Wave B：WebView / media-library / Lottie / Skia 走 adapter，JPush 与 notifications delivery evidence 单独记录；证据关不掉时按 surface 写出 fallback
- catalog 外 native dependency：继续按 `third-party-native-gap` 报出，不降写成普通 matrix drift

## 验收边界

- 本记录不要求真机 device evidence
- 本记录不关闭 release HAP runtime evidence
- 初始 readiness 记录不发布 `1.11.3`，后续发布状态已在本文件对账，二者不可合并改写为单一步骤
- 本记录不改变 true-device / per-capability runtime evidence 要求

## 验证命令

```bash
pnpm build
pnpm docs:sync
git diff --check
pnpm exec jest --runInBand tests/docs.test.ts tests/doctor.test.ts tests/init.test.ts
pnpm test
EXPO_HARMONY_RELEASE_SKIP_HAP=1 pnpm release:check
npm pack --dry-run
```
