# `expo-location` Release Acceptance

- 状态：`pending external release HAP run`
- 当前基线：release evidence 尚未建立
- Route：`/location`
- 目标：确认 release HAP 下定位权限与持续监听路径稳定

## Repo 内已完成前置条件

- route walkthrough：`/location`
- bundle slice：permission、current / watch、heading 与 geocode 主路径已进入 preview sample bundle 检查
- release HAP 前置：release signing、HAP 安装启动与该 route 的 runtime 行为仍需外部验收
- `v1.8.2` app-shell simulator release HAP 证据只覆盖最低安装启动链路，不可替代本记录

## 待填 release 记录

- signing profile：
- 设备 / 模拟器目标：
- HAP artifact：
- 验收时间：
- 执行人：
- 结果：`pass` / `fail`
- 安装 / 启动命令输出：
- 失败日志或截图：

## 必验场景

- release signing 配置生效
- release HAP 安装与启动
- foreground / background permission
- current fix / watch / heading / geocode
- release 模式下不出现权限或后台行为回退
