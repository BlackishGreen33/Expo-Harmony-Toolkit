# `expo-camera` Device Acceptance

- 状态：`pending external device run`
- 当前基线：`bundle=yes[automated]`、`debugBuild=yes[automated]`、`device=yes[manual-doc]`、`release=no[none]`
- Route：`/camera`
- 目标：把 embedded preview、拍照、录像与麦克风权限固化成单 capability device 记录

## Repo 内已完成前置条件

- route walkthrough：`/camera`
- bundle slice：embedded preview、pause / resume、still capture、video recording 与 microphone permission 已进入 preview sample bundle 检查
- permission / shim：`ohos.permission.CAMERA`、`ohos.permission.MICROPHONE` 由 toolkit 生成；`expo-camera` shim 由 toolkit 生成
- debug build evidence source：`debugBuild=yes[automated]`，来源为 preview sample / build gate，不代表真机验收

## 待填设备记录

- 设备型号：
- HarmonyOS / OpenHarmony 版本：
- HAP 类型：debug
- 验收时间：
- 执行人：
- 结果：`pass` / `fail`
- 失败日志或截图：

## 必验场景

- camera / microphone permission
- embedded preview
- `pausePreview` / `resumePreview`
- still photo capture
- video recording start / stop
- denied / canceled / successful result 展示
