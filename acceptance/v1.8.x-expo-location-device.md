# `expo-location` Device Acceptance

- 状态：`pending external device run`
- 当前基线：`bundle=yes[automated]`、`debugBuild=yes[automated]`、`device=yes[manual-doc]`、`release=no[none]`
- Route：`/location`
- 目标：把权限、定位、watch、heading、geocode 全部拆成单 capability device 记录

## Repo 内已完成前置条件

- route walkthrough：`/location`
- bundle slice：permission、current / watch、heading 与 geocode 主路径已进入 preview sample bundle 检查
- permission / shim：`ohos.permission.LOCATION`、`ohos.permission.APPROXIMATELY_LOCATION`、`ohos.permission.LOCATION_IN_BACKGROUND`、`ohos.permission.ACCELEROMETER` 由 toolkit 生成；`expo-location` shim 由 toolkit 生成
- debug build evidence source：`debugBuild=yes[automated]`，来源为 preview sample / build gate，不代表真机验收

## 待填设备记录

- 设备型号：
- HarmonyOS / OpenHarmony 版本：
- HAP 类型：debug
- 验收时间：
- 执行人：
- 结果：`pass` / `fail`
- 失败日志或截图：

## 必验场景

- foreground / background permission
- `getCurrentPositionAsync`
- `watchPositionAsync`
- `getHeadingAsync`
- `watchHeadingAsync`
- `geocodeAsync` / `reverseGeocodeAsync`
